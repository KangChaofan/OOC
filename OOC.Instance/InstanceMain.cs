using System;

namespace OOC.Instance
{
    class InstanceMain
    {
        static void Main(string[] args)
        {
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.WriteLine(System.Guid.NewGuid().ToString());
            Console.ReadLine();
        }
    }
}
